//write a program to find ascii code from user input character . 
#include<stdio.h>
void main()
{
    char p;
    printf("\t ASCII = AMERICAN STANDARD CODE FOR INORMATION INTERCHANGE " );
    printf("\nenter the element ");
    scanf("%c",&p);
    printf("\nyour element is %c and these ASCII code is %d ",p,p);
}