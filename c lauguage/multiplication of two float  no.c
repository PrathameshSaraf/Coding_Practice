#include<stdio.h>
void main()
{
      int l,b,p,a;
      printf("enter the leanth and bredth");
      scanf("%d%d",&l,&b);
      a=l*b;
      printf("the area of rectriangle is %d",a);
      p=2*(l+b);
      printf("\nperimeter of rectriangle is %d",p);
}
